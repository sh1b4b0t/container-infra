# Permite conexões de fora do localhost (necessário para acesso do host macOS)
loopback_users = none

# Limites de recursos
vm_memory_high_watermark.relative = 0.6
disk_free_limit.absolute = 1GB

# Logs
log.console = true
log.console.level = info
